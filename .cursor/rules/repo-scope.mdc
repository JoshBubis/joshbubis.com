---
description: Static portfolio only — Hub owns Studio, contracts, and contact backend
alwaysApply: true
---

# joshbubis.com scope

Edit static public site files here only. Studio/Client Work, Vault, SES, and
Turnstile verify live in `/Users/jbair/Projects/hub`. See `AGENTS.md`.

Work shots are static JPEGs (not live). Bump `?v=` when shipping. Do not stack
`scroll-margin-top` on sections with `scroll-padding-top`.
