---
description: Résumé one-pager only — the studio sales site lives in the josh.menu repo
alwaysApply: true
---

# joshbubis.com scope

This repo is a plain professional **résumé** page for job applications. The studio
("I sell custom websites") moved to `/Users/jbair/Projects/josh.menu` on 2026-07-29.

Do not add — and remove on sight — selling copy, pricing, packages, hire-me calls
to action, the concierge chat widget, or any contact form. Contact is a `mailto:` to
`josh@joshbubis.com`. Never link to `josh.menu` or any studio hostname; the studio
never links here either. That separation is the point.

Styles live in self-contained `resume.css` (not the studio's `style.css`). Bump
`?v=` when shipping.
